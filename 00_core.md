

<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

## FastCF

``` python
acctok = os.environ['CLOUDFLARE_ACC_TOK']
usrtok = os.environ['CLOUDFLARE_USR_TOK']
acctid = os.environ['CLOUDFLARE_ACCT_ID']
acceml = os.environ['CLOUDFLARE_EML_ADD']
```

``` python
base = 'https://api.cloudflare.com/client/v4'
```

``` python
uhdrs = {'Authorization': f'Bearer {usrtok}'}
r = httpx.get(f'{base}/user/tokens/verify', headers=uhdrs)
r.json()['success']
```

    True

``` python
ahdrs = {'Authorization': f'Bearer {acctok}'}
r = httpx.get(f'{base}/accounts/{acctid}/tokens/verify', headers=ahdrs)
r.json()['success']
```

    True

``` python
glbtok = os.environ['CLOUDFLARE_API_KEY']

ghdrs = {'X-Auth-Email': acceml, 'X-Auth-Key': glbtok, 'Content-Type': 'application/json'}
r = httpx.get(f'{base}/user', headers=ghdrs)
r.json()['success']
```

    True

``` python
r = httpx.get(f'{base}/zones', headers=uhdrs)
[(z['name'], z['id'][:8]) for z in r.json()['result'][:1]]
```

    [('aaa.as', '6afde8dc')]

``` python
r = httpx.get(f'{base}/zones', headers=ahdrs, params={'account.id': acctid})
[(z['name'], z['id'][:8]) for z in r.json()['result'][:1]]
```

    [('aaa.as', '6afde8dc')]

## Build endpoint dict

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcflare/blob/main/fastcflare/core.py#L66"
target="_blank" style="float:right; font-size:smaller">source</a>

### build_cf_eps

``` python

def build_cf_eps(
    
):

```

*Call self as a function.*

``` python
eps_test = L(build_cf_eps())
len(eps_test), eps_test[0]
```

    (2575,
     {'path': '/accounts',
      'verb': 'get',
      'data': [],
      'op_id': 'accounts-list-accounts',
      'qparams': [{'name': 'name',
        'description': 'Name of the account.',
        'annotation': str},
       {'name': 'page',
        'description': 'Page number of paginated results.',
        'annotation': int},
       {'name': 'per_page',
        'description': 'Maximum number of results per page.',
        'annotation': int},
       {'name': 'direction',
        'description': 'Direction to order results.',
        'annotation': str}],
      'summary': 'List Accounts',
      'tags': ['Accounts']})

## Create API client

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcflare/blob/main/fastcflare/core.py#L152"
target="_blank" style="float:right; font-size:smaller">source</a>

### CloudflareApi

``` python

def CloudflareApi(
    token:NoneType=None, account_id:NoneType=None, email:NoneType=None, api_key:NoneType=None,
    base_url:str='https://api.cloudflare.com/client/v4'
):

```

*Cloudflare API client supporting both user and account tokens*

``` python
ucf = CloudflareApi(token=usrtok)
ucf.verify().result.status
```

    'active'

``` python
acf = CloudflareApi(token=acctok, account_id=acctid)
acf.verify().result.status
```

    'active'

``` python
ucf('/zones').result[0].name, acf('/zones').result[0].name
```

    ('aaa.as', 'aaa.as')

``` python
zid = ucf('/zones').result[0].id
ucf.dns_records_zone.list_dns_records(zone_id=zid).result[0].name
```

    '*.aaa.as'

``` python
acf.dns_records_zone.list_dns_records(zone_id=zid).result[0].name
```

    '*.aaa.as'

``` python
ucf.zone
```

- zone.get(name: str = None, status: str = None, account_id: str = None,
  account_name: str = None, page: int = None, per_page: int = None,
  order: str = None, direction: str = None, match: str = None): *List
  Zones*
- zone.post(account: dict = None, name: str = None, type: str = ‘full’):
  *Create Zone*
- zone.delete(zone_id): *Delete Zone*
- zone.get_zone_id(zone_id): *Zone Details*
- zone.patch(zone_id, paused: bool = False, plan: dict = None, type: str
  = None, vanity_name_servers: list = \[\]): *Edit Zone*
- zone.put_zones_zone_id_activation_check(zone_id): *Rerun the
  Activation Check*
- zone.purge(zone_id, tags: list = None, hosts: list = None, prefixes:
  list = None, purge_everything: bool = None, files: list = None):
  *Purge Cached Content*

``` python
ucf.zone.get().result[0].name
```

    'aaa.as'

``` python
ucf.dns_records_zone.list_dns_records(zone_id=zid).result[0].name
```

    '*.aaa.as'

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/fastcflare/blob/main/fastcflare/core.py#L188"
target="_blank" style="float:right; font-size:smaller">source</a>

### CloudflareApi.create_token

``` python

def create_token(
    doms, perm_names, name, grp:str='account.zone.'
):

```

*Create a scoped Cloudflare API token for given domains and permission
names*
